{
  "manifest_version": 3,
  "name": "webclaw — DureClaw browser node",
  "version": "0.5.3",
  "description": "DureClaw browser node + Wide Worker workspace: side panel with tasks (= tab groups) and per-task chat that auto-titles itself; browser hands (fetch/DOM/click/fill/JS/download). CORS-free, always-on.",
  "permissions": [
    "offscreen",
    "storage",
    "scripting",
    "tabs",
    "tabGroups",
    "sidePanel",
    "downloads"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "side_panel": {
    "default_path": "sidepanel.html"
  },
  "action": {
    "default_title": "Wide Worker — 작업 패널 열기"
  }
}